# The current Sites runtime injects the D1 database through the DB binding in
# .openai/hosting.json. No application secret is required by the snapshot.
# Keep all real values out of Git.
DATABASE_URL=
API_KEY=
STORAGE_BUCKET=
