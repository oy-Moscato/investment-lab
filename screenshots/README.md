# Screenshots

No screenshots are included in this public snapshot. The current environment
did not expose a stable screenshot-export path for the private production Site,
and screenshots were not allowed to block the source export.

The deployed version was preview-tested through the Sites local preview before
publication. The UI source and responsive CSS remain in `app/page.tsx` and
`app/globals.css`.
